```python
# The task is "Drop the box cutter into the blue box."
# From the image and instruction, we can break this down into 2 stages:
# 1. Grasp the box cutter (keypoint 0)
# 2. Move the box cutter above the blue box and drop it in (keypoints 1 and 2 define the blue box)

num_stages = 2

### stage 1 sub-goal constraints (grasp box cutter)
def stage1_subgoal_constraint1(end_effector, keypoints):
    """Align the end-effector with the box cutter (keypoint 0)."""
    return np.linalg.norm(end_effector - keypoints[0])

### stage 1 path constraints
# No path constraints needed for grasping stage

### stage 2 sub-goal constraints (drop box cutter into blue box)
def stage2_subgoal_constraint1(end_effector, keypoints):
    """The box cutter (keypoint 0) should be 10cm above the center of the blue box (average of keypoints 1 and 2)."""
    box_center = 0.5 * (keypoints[1] + keypoints[2])
    offsetted_point = box_center + np.array([0, 0, 0.10])
    return np.linalg.norm(keypoints[0] - offsetted_point)

### stage 2 path constraints
def stage2_path_constraint1(end_effector, keypoints):
    """The robot must still be grasping the box cutter (keypoint 0)."""
    return get_grasping_cost_by_keypoint_idx(0)

grasp_keypoints = [0, -1]
release_keypoints = [-1, 0]
```