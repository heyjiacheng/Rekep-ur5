```python
# The task is "Grab the game controller."
# This task involves only 1 stage: "grasp game controller"
# The robot needs to align its end-effector with a keypoint on the game controller to grasp it.
# No path constraints are needed for grasping.

num_stages = 1

### stage 1 sub-goal constraints (if any)
def stage1_subgoal_constraint1(end_effector, keypoints):
    """Align the end-effector with the center of the game controller (keypoint 0)."""
    target_point = keypoints[0]
    cost = np.linalg.norm(end_effector - target_point)
    return cost

### stage 1 path constraints (if any)
# No path constraints needed for grasping.

grasp_keypoints = [0]
release_keypoints = [-1]
```